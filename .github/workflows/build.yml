name: Build Single HTML for CodeHS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install

      - name: Build Single HTML
        run: npm run build

      - name: Upload index.html
        uses: actions/upload-artifact@v4
        with:
          name: codehs-index-html
          path: dist/index.html
